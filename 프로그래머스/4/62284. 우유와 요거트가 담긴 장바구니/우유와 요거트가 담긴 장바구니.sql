-- 코드를 입력하세요
SELECT a.CART_ID
FROM
(SELECT 
    CART_ID
FROM
    CART_PRODUCTS 
WHERE
    NAME = 'Milk') a
INNER JOIN
(SELECT 
    CART_ID
FROM
    CART_PRODUCTS 
WHERE
    NAME = 'Yogurt') b ON a.CART_ID = b.CART_ID
GROUP BY CART_ID
ORDER BY CART_ID